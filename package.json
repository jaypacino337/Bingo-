{
  "name": "zsol-site",
  "version": "1.0.0",
  "private": true,
  "description": "zSOL landing page — static, no build step.",
  "scripts": {
    "build": "echo \"Static site — nothing to build.\"",
    "vercel-build": "echo \"Static site — nothing to build.\""
  }
}
